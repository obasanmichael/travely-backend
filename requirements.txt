# Runtime dependencies for the Travely API (Render / production).
# Dev-only tools (ruff, pytest) live in requirements-dev.txt.
# Notebook / eval scripts (matplotlib, etc.) are not required at runtime.

fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.4
pydantic-settings==2.9.1
python-dotenv==1.1.0
python-multipart==0.0.20

firebase-admin==6.8.0
slowapi==0.1.9
sentry-sdk[fastapi]==2.25.1

numpy==2.2.5
pandas==2.2.3
scikit-learn==1.6.1
scikit-fuzzy==0.5.0
